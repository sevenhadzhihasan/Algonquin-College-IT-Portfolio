# Lab 03 - Python Fundamentals Review

This directory contains the completed solutions for the **Python Fundamentals Review Worksheet** from Algonquin College. The lab include iterative logic, data structure manipulation, and flat-file processing (CSV and JSON).

## Implemented Exercises

* **Section A: Functions & Logic** — Implements an iterative menu system running arithmetic calculations (`sum`, `difference`, `product`, `quotient`) wrapped inside a dictionary structure, complete with standard `ZeroDivisionError` error protection.
* **Section B: List Processing** — Demonstrates runtime modifications on data structures including alphabetical sorting patterns and structured sequence iterations.
* **Section C: Mapped Dictionaries** — Establishes keyed reference objects containing student metadata records, applying immediate string transformations.
* **Section D: CSV File Handling** — Employs `csv.DictReader` to stream tabular student record schemas directly out of local text-based spreadsheets into memory instances.
* **Section E: JSON Parsing** — Leverages native data serialization via `json.load()` and `json.dump()` routines to process, dynamically update, and cleanly serialize structured configurations securely back to flat-file storage.

## File Layout

```text
Lab-03-Python-Review/
├── README.md                # This portfolio documentation file
├── worksheet3_solutions.py  # The primary executed Python program code
├── members.csv              # Underlying student relational grid structure
└── books.json               # Flat-file object structure holding persistent logs
```
