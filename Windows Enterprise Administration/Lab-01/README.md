Windows Enterprise Administration - Lab 01
Student Profile
•	Domain Name: dm89069.cst8342.com
•	Server Target Name: Server89069
•	Static Lab Server IP Assigned: 172.16.89.69
Step-by-Step Task Summary

Task 1: Environment Baseline and UI Modifications
•	Installed VMware Tools framework using the automated setup64.exe administrator runtime package.
•	Altered system File Explorer criteria parameters to uncover hidden system extensions and empty local drive icons.

Task 2: Account Provisioning and Access Escalation
•	Provisioned a permanent localized user profile identity using specific laboratory baseline criteria format configurations.
•	Elevated the custom user profile security permissions by binding it into the local machine Administrator authorization group.
•	Activated systemic Remote Desktop features and appended explicit access clearances for both the built-in Administrator and new local identity.

Task 3: Infrastructure Tuning and System Name Modification
•	Restructured local machine properties to switch full host naming conventions directly over to Server89069.
•	Configured the Primary DNS structural suffix criteria variable record string pointing cleanly to dm89069.cst8342.com.
•	Modified server visual parameters over to Best Performance settings and locked custom system paging drive files to exactly 32768 MB.

Task 4: Dual-Homed Network Interface Mapping
•	Isolated and trace-identified local virtual networking interfaces by cross-referencing systemic hardware disconnections inside VMware.
•	Retitled the external public system network adapter link directly to NAT (Blue) and completely deactivated all local IPv6 bindings.
•	Retitled the private lab backend database connection adapter directly to HOST (Red) and dropped local IPv6 hooks.
•	Assigned static host addressing metrics to the HOST (Red) interface using IP target 172.16.89.69 alongside mask 255.255.0.0.
•	Pointed secondary forward parameters to external anchors 8.8.8.8 and 8.8.4.4, then verified live structural routing via command prompt diagnostic tests.

Task 5: Endpoint Optimization and Sandbox Hardening Rules
•	Deactivated real-time local antivirus engines by implementing an active custom configuration state toggle inside the Local Group Policy Editor workspace.
•	Altered Server Manager firewall profile sets to completely disable internal boundary rule blocks across local Private and Domain pathways.
•	Conducted a full cold shutdown of the validated base machine state and initialized a separate standalone Full Clone deployment structure.

Task 6: Directory Infrastructure and DNS Zone Engineering
•	Added authoritative DNS database server roles alongside complete structural administrative management utility modules via Server Manager.
•	Engineered a primary Forward Lookup Zone matching the environment signature structure rule format named cst8342.com.
•	Configured dynamic database criteria modifications to explicitly accept both secure and non-secure transaction data changes.
•	Developed an associated custom structural Reverse Lookup Zone tracking exactly along the local enterprise subnet range parameters of 172.16.
•	Updated foundational forward host target database pointers to systematically auto-generate matching reverse PTR logging strings.
•	Input internet tracking route entry lookups pointing directly out toward upstream internet interface destination target 8.8.8.8.
•	Re-routed all primary network adapter card database client queries right back inward using lookups pointing directly to static interface target 172.16.89.69.
•	Deployed core Active Directory Domain Services environment modules and promoted the server workspace role directly to a Forest Root Domain Controller.
•	Mapped custom user account directory items over into the top-level Domain Admins global container group infrastructure inside the directory layout.
